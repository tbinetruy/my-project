import numpy as np
import pandas as pd

bar = {'a': [i for i in range(len([1, 2, 3])) if i > 1], 'b': "some string", 'c': "some final comments", 'd': "myself as well"}


def test():
    foo = np.array([1, 2, 3])
    return 0